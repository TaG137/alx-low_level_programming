#include <stdio.h>

/**
 * _abs - computes the absolute value of number
 * @n: the interger needed to compute
 *
 * Return: Always 0 (success)
 */
int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
