#include <stdio.h>

/**
 * main - prints the alphabet in lowercase and a
 * new line.
 *
 * Retrun: No return.
 */

void print_alphabet(void)
{
	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar('\n');
	}
}

