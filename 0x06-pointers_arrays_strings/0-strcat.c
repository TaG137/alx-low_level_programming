#include "main.h"

/**
* _strcat - concatenates two stings together
* @dest: a string
* @src: a string
* Return: dest
*/

char *_strcat(char *src, char *dest)
{
  int len, i;
  
  while(len = 0; src[len] != '\0')
  {
    len++;
    _putchar(src[len]);
  }
  return(dest);
}
    
    
