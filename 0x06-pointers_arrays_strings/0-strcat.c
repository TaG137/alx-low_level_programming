#include "main.h"

/**
* _strcat - concatenates two stings together
* @dest: a string
* @src: a string
* Return: dest
*/

char *_strcat(char *src, char *dest)
{
  int j, i;
  
  while(j = 0; src[j] != '\0')
  {
    len++;
    _putchar(src[j]);
  }
  return(dest);
}
    
    
